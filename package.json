{
  "name": "quex-orders-api",
  "version": "1.0.0",
  "private": true,
  "description": "API para gerenciamento de pedidos abertos pelo vendedor no QUÉX.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
